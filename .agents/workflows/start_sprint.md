---
description: VISION — start_sprint (iniciar sprint N; usar con --sprint N)
---

Ejecuta el comando **start_sprint** del framework VISION.

1. Lee `commands/agent_declaration.md` y declara **Agente(s):** en la primera línea de tu respuesta.
2. Abre y sigue **exactamente** `commands/start_sprint.md` en la raíz del repositorio (incluye el uso de `--sprint N`).
3. Ejecuta los scripts Node que indique ese archivo, desde la raíz del proyecto.
4. Informa el resultado y la siguiente acción recomendada.
