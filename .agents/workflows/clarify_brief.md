---
description: VISION — clarify_brief (opcional; antes de generate_brief)
---

Ejecuta el comando **clarify_brief** del framework VISION (invocación opcional por ambigüedad).

1. Lee `commands/agent_declaration.md` y declara **Agente(s):** en la primera línea de tu respuesta.
2. Abre y sigue **exactamente** `commands/clarify_brief.md` en la raíz del repositorio.
3. Ejecuta los scripts Node que indique ese archivo, desde la raíz del proyecto.
4. Informa el resultado; el flujo continúa hacia `generate_brief`.
