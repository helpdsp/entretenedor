---
description: VISION — reset_project (borra artefactos generados; pide confirmación al usuario)
---

Ejecuta el comando **reset_project** del framework VISION.

1. Lee `commands/agent_declaration.md` y declara **Agente(s):** en la primera línea de tu respuesta.
2. Abre y sigue **exactamente** `commands/reset_project.md` en la raíz del repositorio (incluye advertencia y confirmación).
3. Ejecuta los scripts Node que indique ese archivo, desde la raíz del proyecto.
4. Informa el resultado y la siguiente acción recomendada.
