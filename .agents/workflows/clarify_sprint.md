---
description: VISION — clarify_sprint (opcional; antes de start_sprint con --sprint N)
---

Ejecuta el comando **clarify_sprint** del framework VISION (invocación opcional por ambigüedad).

1. Lee `commands/agent_declaration.md` y declara **Agente(s):** en la primera línea de tu respuesta.
2. Abre y sigue **exactamente** `commands/clarify_sprint.md` en la raíz del repositorio (incluye `--sprint N`).
3. Ejecuta los scripts Node que indique ese archivo, desde la raíz del proyecto.
4. Informa el resultado; el flujo continúa hacia `start_sprint`.
