---
description: VISION — continue_sprint (retomar sprint activo incompleto)
---

Ejecuta el comando **continue_sprint** del framework VISION.

1. Abre y sigue **exactamente** `commands/continue_sprint.md` en la raíz del repositorio (incluye `--sprint N` opcional).
2. Declara agente(s) según `commands/agent_declaration.md`.
