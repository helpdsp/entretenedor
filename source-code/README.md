# Source Code Input

Place here the source files to analyze when the project is in reverse engineering mode.

- Required when reverse engineering is enabled.
- Used by `generate_brief` as additional input context.
